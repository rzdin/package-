pipeline {
  agent any
  tools {nodejs "NODEJS"}
 
  stages {
    stage('build') {
      steps {
          sh 'npm install'
    
      }
    }
    stage('package') {
      steps {
           zip zipFile: 'hello-serverless',  dir:/var/lib/jenkins/workspace/my-package-jenkins-job
     }
   }
 }
}
